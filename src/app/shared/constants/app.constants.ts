/** Maximum number of options per survey. */
export const MAX_SURVEY_OPTIONS = 10;

/** Minimum number of options per survey. */
export const MIN_SURVEY_OPTIONS = 2;

/** Maximum title length for a survey. */
export const MAX_TITLE_LENGTH = 120;

/** Maximum description length for a survey. */
export const MAX_DESCRIPTION_LENGTH = 500;

/** Threshold in milliseconds for "urgent" surveys (3 days). */
export const URGENT_THRESHOLD_MS = 3 * 24 * 60 * 60 * 1000;

/** Toast auto-dismiss duration in milliseconds. */
export const TOAST_DURATION_MS = 3000;
